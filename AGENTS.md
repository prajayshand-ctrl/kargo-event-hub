<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

# kargo-event-hub

Next.js App Router application. **The README is still the unmodified `create-next-app` boilerplate**,
so it tells you nothing about this repo specifically; do not treat it as a description of the
project.

Everything above this line is generated by `next dev` and is re-created automatically. Leave it
alone; add repo guidance below.

## Commands

```sh
npm run dev
npm run build
npm run start
npm run lint
```

## Layout

`app/` (App Router routes and layouts), `public/`, with TypeScript config in `tsconfig.json`,
lint config in `eslint.config.mjs`, Tailwind via `postcss.config.mjs`, and `next.config.ts`.

## State of the repo

Small and early. There is **no test suite, no CI workflow, and no deploy configuration** in the repo.
Before assuming a convention exists, check whether it is actually present; much of the structure is
still the scaffold.

If you add anything substantial here, it is worth replacing the boilerplate README and expanding this
section at the same time.
